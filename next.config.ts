import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // output: "export"
  images: {
    domains: ['backend.novatradar.com'], // Add your domain here
  }
};

export default nextConfig;
